# DigitalOcean Functions env — used by `doctl serverless deploy` to fill the
# ${...} placeholders in project.yml. Copy to functions/.env (gitignored) and
# fill in real values. These are server-side only; never exposed to the browser.

RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxx

# Inbox(es) that receive the leads. Comma-separate for multiple:
#   LEAD_NOTIFY_EMAIL=michael@ygtoronto.com, andrew@me.com
LEAD_NOTIFY_EMAIL=michael@ygtoronto.com

# Optional BCC — a BLIND copy (hidden from the other recipients). Comma-separate.
LEAD_BCC_EMAIL=andrew@me.com

FROM_EMAIL=YardGuard Quotes <quotes@ygtoronto.com>
ALLOWED_ORIGIN=https://ygtoronto.com

# Best-effort warm-instance abuse controls (defaults: 5 requests / 10 minutes
# per proxy-provided client address). Durable edge rate limiting is still a
# future deployment concern once traffic warrants it.
QUOTE_RATE_LIMIT_MAX=5
QUOTE_RATE_LIMIT_WINDOW_MS=600000

# NOTE: the optional CC (visible copy) is set in project.yml, not here, because
# doctl can't substitute an empty .env value. See LEAD_CC_EMAIL there.
